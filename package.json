{
  "name": "core-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-up": "docker-compose up -d",
    "start": "npm run docker-up && npm run start -w @nlxi/*"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "@nlxi/app-module"
  ]
}
